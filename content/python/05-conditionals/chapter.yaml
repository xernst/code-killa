number: 5
slug: conditionals
title: "conditionals — where ai silently bugs"
blurb: "`if` looks simple. the traps inside it — empty values, `==` vs `is`, the difference between `0` and `None` — are where ai quietly ships wrong code."
lessons:
  - 01-truthiness-bugs
  - 02-elif-and-pattern-match
