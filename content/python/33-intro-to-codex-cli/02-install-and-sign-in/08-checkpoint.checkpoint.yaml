xp: 6
estSeconds: 150
concept: installing-codex
prompt: |
  Lesson two lock-in. The starter declares three variables for the
  Codex setup steps. Write three `print(f"...")` lines so the output is
  exactly:

  ```
  install: brew on a mac, or npm which needs node first
  verify: codex --version prints a version number
  sign in: codex login, a browser approval with your chatgpt account
  ```

  Type the three lines and run it.
starter: |
  install = "brew on a mac, or npm which needs node first"
  verify = "codex --version prints a version number"
  sign_in = "codex login, a browser approval with your chatgpt account"

  # write three print() lines below using f-strings.
  # the grader expects the three lines shown above, exactly.
grader:
  kind: stdout-equality
  expected: |
    install: brew on a mac, or npm which needs node first
    verify: codex --version prints a version number
    sign in: codex login, a browser approval with your chatgpt account
  normalize: collapse-trailing-newline
solution: |
  install = "brew on a mac, or npm which needs node first"
  verify = "codex --version prints a version number"
  sign_in = "codex login, a browser approval with your chatgpt account"

  print(f"install: {install}")
  print(f"verify: {verify}")
  print(f"sign in: {sign_in}")
passThreshold: 0.66
hint:
  - level: 1
    body: "Same pattern as every checkpoint: `print(f\"label: {variable}\")`. The third label is `sign in` with a space, but the variable is `sign_in` with an underscore, since variable names can't have spaces."
  - level: 2
    body: |
      Here are the three lines:

      ```python
      print(f"install: {install}")
      print(f"verify: {verify}")
      print(f"sign in: {sign_in}")
      ```
